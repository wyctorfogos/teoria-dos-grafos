To run the scripts, just choose it (the wanted problem) and run the command below:

For example: Trying to solve the problem '726798-A'
```bash
    python3 trabalho-2/questoes/src/scripts/questao_726798-A.py
```